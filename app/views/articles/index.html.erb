<h1>All Articles</h1>

<ul class="articles">
  <% @articles.each do |article| %>
    <li>
        <h4><%= link_to article.title, article_path(article), class: 'article-title' %></h4>
    </li>
  <% end %>
</ul>
<%= link_to "Create a New Article", new_article_path, class: "new_article" %>
